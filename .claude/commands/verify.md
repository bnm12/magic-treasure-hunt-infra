# /verify

Verify that the project conforms to the vision, protocols, and standard guidelines.

1. Check that all recent code changes are documented in the source-of-truth markdown files in the `docs/` directory.
2. Verify that `AGENTS.md` is updated if component responsibilities have changed.
3. Verify that the on-tag data model continues to respect the 8-byte payload and free record 1 constraint.
