# Tryllestavsprojekt 🪄

@AGENTS.md

## Build and Test Commands

* **Install dependencies:** `npm install` (run in `website/`)
* **Dev server:** `npm run dev` (run in `website/`)
* **Production build:** `npm run build` (run in `website/`)
* **Run tests:** `npm run test` (run in `website/`)
